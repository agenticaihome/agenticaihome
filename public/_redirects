# Cloudflare Pages SPA routing
# Real pages (detail, create, analytics) are auto-served by CF from .html files
# Only dynamic agent routes need placeholder shells
/agents/:id /agents/placeholder.html 200
